import { CustomBtnDirective } from './custom-btn.directive';

describe('CustomBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomBtnDirective();
    expect(directive).toBeTruthy();
  });
});
